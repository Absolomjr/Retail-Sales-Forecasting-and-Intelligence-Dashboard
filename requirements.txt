# Notebook + training environment.
# The API has its own, leaner list in backend/requirements.txt.

# core
numpy>=1.26
pandas>=2.2,<3          # mlflow does not yet support pandas 3
pyarrow>=15

# notebook
jupyterlab>=4.2
ipykernel>=6.29
matplotlib>=3.8
seaborn>=0.13

# modelling
scikit-learn>=1.4
xgboost>=2.1
lightgbm>=4.5
optuna>=4.0
shap>=0.46
joblib>=1.3

# tracking
mlflow>=3,<4

# serving (so the notebook can import the same code the API runs)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
SQLAlchemy>=2.0
psycopg[binary]>=3.1

# tests
pytest>=8.0
httpx>=0.27
